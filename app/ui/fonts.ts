// Import Google Fonts
import { Inter, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
    weight: ['400', '700'], // Specify desired weights (e.g., normal and bold)
    subsets: ['latin'], // Required subset for Google Fonts
});